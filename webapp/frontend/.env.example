# Copy to .env and adjust if your backend runs on a different host/port.
# This is the only env var the frontend reads; everything else flows through
# the API responses (download URLs, status, etc.).
VITE_API_BASE_URL=http://localhost:8000
